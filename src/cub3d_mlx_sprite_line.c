#include "cub3d.h"
#include "cub3d_draw.h"

void	ft_mlx_put_spr_line(t_draw_data *data, t_sprites *temp, t_sprite *spr)
{
	int		v_line;

	v_line = temp->drawStartX;
	while (v_line < temp->drawEndX)
	{

		++v_line;
	}
}
